<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">

        <item android:id="@+id/principal"
            android:icon="@drawable/ic_baseline_principal"
            android:title="Principal"/>
        <item android:id="@+id/servicos"
            android:icon="@drawable/ic_baseline_servicos"
            android:title="Serviços"/>
        <item android:id="@+id/clientes"
            android:icon="@drawable/ic_baseline_clientes"
            android:title="Clientes"/>

    </group>
    <item android:title="Entre em contato">
        <menu>

            <item android:id="@+id/contato"
                android:icon="@drawable/ic_baseline_contato"
                android:title="Contato"/>

            <item android:id="@+id/sobre"
                android:icon="@drawable/ic_baseline_sobre"
                android:title="Sobre"/>

        </menu>
    </item>

</menu>